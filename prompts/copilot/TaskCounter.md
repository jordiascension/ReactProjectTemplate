Create a new React 16 class component in a file called TaskCounter.jsx.

Create a new file at src/components/TaskCounterNew.jsx and write a React 16 class component in it.

The component should:
- Be a class component
- Store a list of tasks in state
- Each task should have id, text, and completed
- Display the tasks with checkboxes
- Update the completed value when a checkbox is clicked
- Show the number of completed tasks
- Use simple inline styles
- Export the component as default


Do not only show me the code. Apply the changes to the workspace.
To complete your request, please enable file editing tools, and I can create the file for you automatically.