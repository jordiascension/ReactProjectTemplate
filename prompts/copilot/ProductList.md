Create two React components using Material UI: ProductList and ProductItem.
ProductList receives an array of products as props.
Each product has id, name and price.
Render the products using Material UI List, ListItem, ListItemText, Paper and Typography.
Use ProductItem to render each product.
Use the product id as the key.

Create a Jest snapshot test using react-test-renderer for ProductList.
Render ProductList with three sample products.
Verify that all products are rendered and the component matches the snapshot