Create a React component called UserCard using Material UI in a folder called like the component name.
The component receives name, email, rol and avatarUrl as props.
Use Card, CardContent, Avatar, Typography and Stack.
Display the avatar, user name, email and role.
Keep the component simple and reausable.

Create a Jest snapshot test using react-test-renderer for UserCard.
Render the component with sample props: name, email, role and avatarUrl.
Verify that the component matches the snapshot.