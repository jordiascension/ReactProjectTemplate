Create a new file at src/components/TaskCounterNew.jsx.

In that file, create a React functional component named TaskCounterNew.

Requirements:
- Use a functional component, not a class component.
- Use React hooks.
- Store a list of tasks using useState.
- Each task must have id, text, and completed.
- Display the tasks with checkboxes.
- When a checkbox is clicked, update that task's completed value in state.
- Show the number of completed tasks.
- Use simple inline styles.
- Export the component as default.

Apply the changes directly to the workspace. Do not only show me the code.